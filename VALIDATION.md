# 验证记录

2026-09-08，Apple Silicon macOS。

- 使用自有原版包与本地 Zulu 8.0.504 FX 完成安装脚本端到端构建。
- 构建后的 JAR 仅三个预期类变化，其余条目一致。
- 将生成目录移动到包含中文及空格的新路径后，两个 JAR 校验通过，内置 Java 正常执行。
- 输出目录已存在时拒绝安装，不覆盖数据。
- 原版 JAR 哈希不匹配时拒绝安装，不生成输出目录。
- Python 帮助入口、Shell 语法检查、Git whitespace 检查通过。
- 提交仅含源码、模板、文档；未提交原厂组件、运行环境、配置、数据库、补丁包、诊断日志或截图。

此前本机适配版本已验证主界面、补丁管理页面渲染及重复启动保护。此次分发脚本验证未启动业务应用，未连接业务服务器或执行补丁操作。下载分支所用官方运行环境归档在首次适配时已校验 SHA-256；此次通过本地运行环境参数构建，没有重新下载。

未验证 Intel Mac，也未在第二台 Mac 上运行。联网补丁查询、安装、备份与回退仍需目标环境验收。
